apiVersion: v1
kind: Service
metadata:
  name: {{ include "shadow-account-watcher.fullname" . }}
  labels:
    {{- include "shadow-account-watcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shadow-account-watcher.selectorLabels" . | nindent 4 }}
