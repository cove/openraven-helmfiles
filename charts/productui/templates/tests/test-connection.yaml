apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "productui.fullname" . }}-test-connection"
  labels:
{{ include "productui.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "productui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
