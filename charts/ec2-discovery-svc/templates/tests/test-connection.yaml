apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ec2-discovery-svc.fullname" . }}-test-connection"
  labels:
{{ include "ec2-discovery-svc.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ec2-discovery-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
