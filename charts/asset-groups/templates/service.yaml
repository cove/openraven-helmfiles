apiVersion: v1
kind: Service
metadata:
  name: {{ include "asset-groups.fullname" . }}
  labels:
{{ include "asset-groups.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "asset-groups.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
