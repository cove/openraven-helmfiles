apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arcade-analytics.fullname" . }}-test-connection"
  labels:
{{ include "arcade-analytics.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "arcade-analytics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
