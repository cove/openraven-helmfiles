apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shadow-account-watcher.fullname" . }}-test-connection"
  labels:
{{ include "shadow-account-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "shadow-account-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
