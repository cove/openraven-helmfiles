apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "account-management.fullname" . }}-test-connection"
  labels:
{{ include "account-management.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "account-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
