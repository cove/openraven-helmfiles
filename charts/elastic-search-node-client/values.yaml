# Default values for elastic-search-node-client.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  registry: registry.gitlab.com
  repository: openraven/open/elastic-search-node-client
  # Pulled from minor value in Chart.yaml version:
  tag: ~
  pullPolicy: IfNotPresent

imagePullSecrets: []
# - name: some-pull-secret

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

elastic_url: "http://elasticsearch-es-http.elasticsearch.svc.cluster.local:9200"
elastic_username: ~
elastic_password: ~

kibana_url: "http://kibana-kb-http.elasticsearch.svc.cluster.local:5601/kibana"

global:
  groupId: ~

sentry_dsn: https://7188f75192984a8bbc36a212ab341b2c@o322024.ingest.sentry.io/5270430

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/auth-url: "https://$host/oauth2/auth"
    nginx.ingress.kubernetes.io/auth-signin: "https://$host/oauth2/start?rd=$escaped_request_uri"
    nginx.ingress.kubernetes.io/auth-response-headers: "x-auth-request-user, x-auth-request-email, authorization, x-auth-request-access-token"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header cookie "";
  hosts:
    - host: #Check in ingress.yml
      paths: ["/es"]

  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  requests:
    memory: 512Mi
    cpu: 200m
  limits:
    memory: 512Mi
    cpu: 500m

nodeSelector: {}

tolerations: []

affinity: {}

elasticsearch_load:
  image: "elasticdump/elasticsearch-dump:v6.28.4"
  url: "http://elasticsearch-es-http.elasticsearch.svc.cluster.local:9200"
  bucketname: ~
  keyprefix: "es"
