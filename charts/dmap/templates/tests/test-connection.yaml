apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dmap.fullname" . }}-test-connection"
  labels:
{{ include "dmap.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dmap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
