apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "s3-scan-service.fullname" . }}-test-connection"
  labels:
{{ include "s3-scan-service.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "s3-scan-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
