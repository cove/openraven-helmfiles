apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graph-explorer.fullname" . }}-test-connection"
  labels:
{{ include "graph-explorer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "graph-explorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
