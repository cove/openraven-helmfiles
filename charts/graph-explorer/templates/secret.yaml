apiVersion: v1
kind: Secret
metadata:
  name: {{ template "graph-explorer.fullname" . }}-secret
  labels:
    app: {{ template "graph-explorer.name" . }}
    chart: {{ template "graph-explorer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  root-password: {{ .Values.odbRootPassword | b64enc | quote }}
