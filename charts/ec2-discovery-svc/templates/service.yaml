apiVersion: v1
kind: Service
metadata:
  name: {{ include "ec2-discovery-svc.fullname" . }}
  labels:
{{ include "ec2-discovery-svc.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ec2-discovery-svc.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
