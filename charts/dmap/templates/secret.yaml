apiVersion: v1
kind: Secret
metadata:
  name: {{ template "dmap.fullname" . }}-secret
  labels:
    app: {{ template "dmap.name" . }}
    chart: {{ template "dmap.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  root-password: {{ .Values.odbRootPassword | b64enc | quote }}
