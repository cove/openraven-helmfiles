apiVersion: v1
kind: Secret
metadata:
  name: {{ template "ec2-discovery-svc.fullname" . }}-secret
  labels:
    app: {{ template "ec2-discovery-svc.name" . }}
    chart: {{ template "ec2-discovery-svc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  root-password: {{ .Values.odbRootPassword | b64enc | quote }}
