apiVersion: v1
kind: Service
metadata:
  name: {{ include "integrations.fullname" . }}
  labels:
{{ include "integrations.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "integrations.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
