apiVersion: v1
kind: Secret
metadata:
  name: {{ template "s3-discovery-svc.fullname" . }}-secret
  labels:
    app: {{ template "s3-discovery-svc.name" . }}
    chart: {{ template "s3-discovery-svc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  root-password: {{ .Values.odbRootPassword | b64enc | quote }}
