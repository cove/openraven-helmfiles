apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3-scan-service.fullname" . }}
  labels:
{{ include "s3-scan-service.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "s3-scan-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
